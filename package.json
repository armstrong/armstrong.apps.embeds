{
    "name": "armstrong.apps.embeds",
    "version": "0.8",
    "description": "Provides a way to store, relate to, extract metadata and embed content from external URLs via a modular backend system",
    "install_requires": [
        "django-extensions>=1.0.3",
        "django-model-utils>=1.1.0",
        "embedly>=0.5.0"
    ]
}
